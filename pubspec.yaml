name: reelity
description: "Tu vida convertida en un reality show"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Navegación
  go_router: ^13.0.0

  # UI
  google_fonts: ^6.1.0

  # Almacenamiento local
  shared_preferences: ^2.2.2

  # WebView para reproducir YouTube
  flutter_inappwebview: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true